function reduction_table_actions(anticache=-1) {
        // Setup Delete Frame
        setup_delete_frame();
        // Setup Select Meteor
        setup_select_meteor(anticache);
        // Missing Thumbs?
        //setTimeout(function() { test_missing_thumb(); }, 2000);
}
